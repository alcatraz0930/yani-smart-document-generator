const form=document.querySelector('#docForm'), output=document.querySelector('#output'), empty=document.querySelector('#empty'), generateBtn=document.querySelector('#generateBtn'), copyBtn=document.querySelector('#copyBtn'), downloadBtn=document.querySelector('#downloadBtn');
const val=id=>document.querySelector(id).value;
form.addEventListener('submit',async e=>{e.preventDefault();generateBtn.disabled=true;generateBtn.innerHTML='Generating… <span>✦</span>';try{const r=await fetch('/api/generate',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({documentType:val('#documentType'),title:val('#title'),recipient:val('#recipient'),details:val('#details'),tone:val('#tone')})});const data=await r.json();if(!r.ok)throw new Error(data.error||'Generation failed');empty.hidden=true;output.hidden=false;output.value=data.document;copyBtn.disabled=false;downloadBtn.disabled=false}catch(err){alert(err.message)}finally{generateBtn.disabled=false;generateBtn.innerHTML='Generate Document <span>→</span>'}});
document.querySelector('#exampleBtn').addEventListener('click',()=>{document.querySelector('#documentType').value='Business Letter';document.querySelector('#title').value='Follow-up after workflow consultation';document.querySelector('#recipient').value='Prospective client';document.querySelector('#details').value='Thank the client for meeting with us yesterday. Confirm that we discussed automating their weekly reporting process, which currently takes about four hours every Friday. Mention that the next step is for us to review their existing spreadsheet template and data sources. Ask them to send those files when convenient. Keep the message professional and warm.';document.querySelector('#tone').value='Professional';document.querySelector('#details').focus()});
copyBtn.addEventListener('click',async()=>{await navigator.clipboard.writeText(output.value);copyBtn.textContent='Copied ✓';setTimeout(()=>copyBtn.textContent='Copy',1500)});
downloadBtn.addEventListener('click',()=>{const blob=new Blob([output.value],{type:'text/plain;charset=utf-8'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='yani-smart-document.txt';a.click();URL.revokeObjectURL(a.href)});
